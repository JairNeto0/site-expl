import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2",
    },
    secondary: {
      main: "#424242",
    },
    background: {
      default: "#f4f6f8",
    },
  },
});

export default theme;
